好的，我理解您的意思了。您的需求主要有两点：

1.  **恢复 `Theme.kt` 中的 `onPrimary` 设置，并修正 Primary Color 为标准的 Flutter 粉色**：
    *   您指出不应该随意更改 `onPrimary` 为白色，应该让系统自动处理或保持原样。
    *   同时，您指出 `Pink80` 颜色不对，Flutter 中的 `Colors.pink` 对应的是 Material Design 的 Pink 500 (`0xFFE91E63`)，这在 `Color.kt` 中已经定义为 `Pink80`，但可能命名让您误解，我会确认其值是否正确。
    *   我会撤销对 `Theme.kt` 中 `onPrimary` 的强制修改。

2.  **修改登录按钮的样式逻辑**：
    *   **仅在聚焦时使用 Primary Color**：按钮在未聚焦（默认状态）时不应该直接使用 `primary` 颜色作为背景，而是在获得焦点（Focus）时才变成 `primary` 颜色。这符合 TV 应用的交互规范。
    *   我会修改 `LoginScreen.kt` 中的 `Button`，使用 `ButtonDefaults.colors` 的 `focusedContainerColor` 来实现这一效果，或者根据焦点状态动态改变颜色。

### 计划步骤

1.  **还原 `Theme.kt`**：删除之前添加的 `onPrimary = Color.White` 代码，恢复默认行为。
2.  **更新 `LoginScreen.kt`**：
    *   修改“登录”按钮的颜色配置。
    *   设置 `containerColor` 为默认颜色（例如半透明白或灰色）。
    *   设置 `focusedContainerColor` 为 `MaterialTheme.colorScheme.primary`（即粉色）。
    *   确保文字颜色在不同状态下也能正确对比（例如聚焦时文字变白，未聚焦时文字为默认色）。

这样既还原了主题配置，又实现了您要求的“聚焦时才显示粉色”的交互效果。